#include <stdio.h>

int main() {

  int grade = 90;
  
  // Write an if statement that checks if grade >= 60 is true and prints “Pass\n” if it is. Otherwise if grade < 60 it prints “Fail”
  // Modify grade to be 55 and check the output of the program

  return 0;
  
}

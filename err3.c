#include <stdio.h>

int main (){

int a, b;
char ch;

printf("Give a character: ");
scanf("%d", &ch);

printf("Give two integer numbers: ");
scanf("%d %d", a, b);

if (ch = 'A')
    printf("Your input was %d, %d, and %c", a, b, ch);
else
    printf(You did not input A as the character);

return 0;

}
